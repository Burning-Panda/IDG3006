module.exports = {
    // Access token is used for 2factor validation with the authorization token below, only for post functions,
    access_token: "",

    // Password is used to validate all get requests:
    authorization_token: ""
}